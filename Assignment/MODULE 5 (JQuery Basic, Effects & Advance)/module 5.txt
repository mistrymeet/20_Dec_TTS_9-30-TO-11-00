Question 1 :- What is JQuery?
Answer :- 
          jQuery is an open source JavaScript library that simplifies the interactions between an HTML/CSS document, or more precisely the Document Object Model (DOM), and JavaScript. 

          Elaborating the terms, jQuery simplifies HTML document traversing and manipulation, browser event handling, DOM animations, Ajax interactions, and cross-browser JavaScript development.

          syntex :
                   $(document).ready(function(){
                    $(selector).event(function(){
                        $(selector).action
                    })
                   })

Question 2 :- How to Apply CSS Using JQuery, How to Add Class and Remove Class in JQuery, JQuery Animation?
Answer :-
          addClass() - Adds one or more classes to the selected elements.
          removeClass() - Removes one or more classes from the selected elements.
          toggleClass() - Toggles between adding/removing classes from the selected elements.
          css() - Sets or returns the style attribute. 

Question 3 :- How to create slider with animation?  
Answer :-         
          we can use several sliders such as bxslider, swiper js, owl carousel etc.. and then

          Update the background image in each slide
          Update the text layers in each slide
          Edit the button text and design in each slide
          Edit the slider navigation